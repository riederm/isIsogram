#include "isogram.h"
#include <stdio.h>
#include <string.h>


bool is_isogram(const char phrase[]) {
    // This function should:
    // - Check if the given phrase is an isogram (a word or phrase without repeating letters).
    // - Be case-insensitive (e.g., 'A' and 'a' should be considered the same).

    return false;
}

